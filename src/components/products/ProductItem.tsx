import { ProductItemProps } from "../../types/components/products";

export default function ProductItem({
  id,
  brand,
  category,
  images,
  price,
  rating,
  title,
  tags,
  discountPercentage,
}: ProductItemProps) {
  return <div>ProductItem</div>;
}
